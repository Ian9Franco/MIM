{
  "name": "@mim/desktop",
  "version": "11.4.6",
  "description": "Minecraft Instance Manager Desktop application runtime & Electron packaging",
  "private": true,
  "main": "../../standalone/main.js",
  "scripts": {
    "build": "next build",
    "prepare:standalone": "node ../../standalone/prepare.js",
    "start": "electron ../../standalone/main.js",
    "package:win": "npm run build && npm run prepare:standalone && electron-builder build --win --publish never"
  },
  "dependencies": {
    "@mim/contracts-core": "*",
    "@mim/network-resilience": "*",
    "@mim/server-engine": "*"
  }
}
